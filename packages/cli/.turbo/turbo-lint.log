$ biome lint .
src/__tests__/copy.test.ts:2:29 lint/style/useNodejsImportProtocol  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━

  i A Node.js builtin module should be imported with the node: protocol.
  
    1 │ import { afterAll, beforeAll, describe, expect, it } from "bun:test";
  > 2 │ import { mkdtemp, rm } from "fs/promises";
      │                             ^^^^^^^^^^^^^
    3 │ import { tmpdir } from "os";
    4 │ import { join } from "path";
  
  i Using the node: protocol is more explicit and signals that the imported module belongs to Node.js.
  
  i Unsafe fix: Add the node: protocol.
  
     1  1 │   import { afterAll, beforeAll, describe, expect, it } from "bun:test";
     2    │ - import·{·mkdtemp,·rm·}·from·"fs/promises";
        2 │ + import·{·mkdtemp,·rm·}·from·"node:fs/promises";
     3  3 │   import { tmpdir } from "os";
     4  4 │   import { join } from "path";
  

src/__tests__/copy.test.ts:3:24 lint/style/useNodejsImportProtocol  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━

  i A Node.js builtin module should be imported with the node: protocol.
  
    1 │ import { afterAll, beforeAll, describe, expect, it } from "bun:test";
    2 │ import { mkdtemp, rm } from "fs/promises";
  > 3 │ import { tmpdir } from "os";
      │                        ^^^^
    4 │ import { join } from "path";
    5 │ import { copyFilesToProject } from "../steps/copy";
  
  i Using the node: protocol is more explicit and signals that the imported module belongs to Node.js.
  
  i Unsafe fix: Add the node: protocol.
  
     1  1 │   import { afterAll, beforeAll, describe, expect, it } from "bun:test";
     2  2 │   import { mkdtemp, rm } from "fs/promises";
     3    │ - import·{·tmpdir·}·from·"os";
        3 │ + import·{·tmpdir·}·from·"node:os";
     4  4 │   import { join } from "path";
     5  5 │   import { copyFilesToProject } from "../steps/copy";
  

src/__tests__/copy.test.ts:4:22 lint/style/useNodejsImportProtocol  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━

  i A Node.js builtin module should be imported with the node: protocol.
  
    2 │ import { mkdtemp, rm } from "fs/promises";
    3 │ import { tmpdir } from "os";
  > 4 │ import { join } from "path";
      │                      ^^^^^^
    5 │ import { copyFilesToProject } from "../steps/copy";
    6 │ import { fileExists } from "../utils/files";
  
  i Using the node: protocol is more explicit and signals that the imported module belongs to Node.js.
  
  i Unsafe fix: Add the node: protocol.
  
     2  2 │   import { mkdtemp, rm } from "fs/promises";
     3  3 │   import { tmpdir } from "os";
     4    │ - import·{·join·}·from·"path";
        4 │ + import·{·join·}·from·"node:path";
     5  5 │   import { copyFilesToProject } from "../steps/copy";
     6  6 │   import { fileExists } from "../utils/files";
  

src/__tests__/files.test.ts:2:50 lint/style/useNodejsImportProtocol  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━

  i A Node.js builtin module should be imported with the node: protocol.
  
    1 │ import { afterAll, beforeAll, describe, expect, it } from "bun:test";
  > 2 │ import { mkdtemp, readFile, rm, writeFile } from "fs/promises";
      │                                                  ^^^^^^^^^^^^^
    3 │ import { tmpdir } from "os";
    4 │ import { join } from "path";
  
  i Using the node: protocol is more explicit and signals that the imported module belongs to Node.js.
  
  i Unsafe fix: Add the node: protocol.
  
     1  1 │   import { afterAll, beforeAll, describe, expect, it } from "bun:test";
     2    │ - import·{·mkdtemp,·readFile,·rm,·writeFile·}·from·"fs/promises";
        2 │ + import·{·mkdtemp,·readFile,·rm,·writeFile·}·from·"node:fs/promises";
     3  3 │   import { tmpdir } from "os";
     4  4 │   import { join } from "path";
  

src/__tests__/files.test.ts:3:24 lint/style/useNodejsImportProtocol  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━

  i A Node.js builtin module should be imported with the node: protocol.
  
    1 │ import { afterAll, beforeAll, describe, expect, it } from "bun:test";
    2 │ import { mkdtemp, readFile, rm, writeFile } from "fs/promises";
  > 3 │ import { tmpdir } from "os";
      │                        ^^^^
    4 │ import { join } from "path";
    5 │ import {
  
  i Using the node: protocol is more explicit and signals that the imported module belongs to Node.js.
  
  i Unsafe fix: Add the node: protocol.
  
     1  1 │   import { afterAll, beforeAll, describe, expect, it } from "bun:test";
     2  2 │   import { mkdtemp, readFile, rm, writeFile } from "fs/promises";
     3    │ - import·{·tmpdir·}·from·"os";
        3 │ + import·{·tmpdir·}·from·"node:os";
     4  4 │   import { join } from "path";
     5  5 │   import {
  

src/__tests__/files.test.ts:4:22 lint/style/useNodejsImportProtocol  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━

  i A Node.js builtin module should be imported with the node: protocol.
  
    2 │ import { mkdtemp, readFile, rm, writeFile } from "fs/promises";
    3 │ import { tmpdir } from "os";
  > 4 │ import { join } from "path";
      │                      ^^^^^^
    5 │ import {
    6 │   ensureDir,
  
  i Using the node: protocol is more explicit and signals that the imported module belongs to Node.js.
  
  i Unsafe fix: Add the node: protocol.
  
     2  2 │   import { mkdtemp, readFile, rm, writeFile } from "fs/promises";
     3  3 │   import { tmpdir } from "os";
     4    │ - import·{·join·}·from·"path";
        4 │ + import·{·join·}·from·"node:path";
     5  5 │   import {
     6  6 │     ensureDir,
  

src/__tests__/paths.test.ts:2:25 lint/style/useNodejsImportProtocol  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━

  i A Node.js builtin module should be imported with the node: protocol.
  
    1 │ import { describe, expect, it } from "bun:test";
  > 2 │ import { homedir } from "os";
      │                         ^^^^
    3 │ import { resolve } from "path";
    4 │ import { expandTilde, resolvePath, trimTrailingSlash } from "../utils/paths";
  
  i Using the node: protocol is more explicit and signals that the imported module belongs to Node.js.
  
  i Unsafe fix: Add the node: protocol.
  
     1  1 │   import { describe, expect, it } from "bun:test";
     2    │ - import·{·homedir·}·from·"os";
        2 │ + import·{·homedir·}·from·"node:os";
     3  3 │   import { resolve } from "path";
     4  4 │   import { expandTilde, resolvePath, trimTrailingSlash } from "../utils/paths";
  

src/__tests__/paths.test.ts:3:25 lint/style/useNodejsImportProtocol  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━

  i A Node.js builtin module should be imported with the node: protocol.
  
    1 │ import { describe, expect, it } from "bun:test";
    2 │ import { homedir } from "os";
  > 3 │ import { resolve } from "path";
      │                         ^^^^^^
    4 │ import { expandTilde, resolvePath, trimTrailingSlash } from "../utils/paths";
    5 │ 
  
  i Using the node: protocol is more explicit and signals that the imported module belongs to Node.js.
  
  i Unsafe fix: Add the node: protocol.
  
     1  1 │   import { describe, expect, it } from "bun:test";
     2  2 │   import { homedir } from "os";
     3    │ - import·{·resolve·}·from·"path";
        3 │ + import·{·resolve·}·from·"node:path";
     4  4 │   import { expandTilde, resolvePath, trimTrailingSlash } from "../utils/paths";
     5  5 │   
  

src/steps/copy.ts:2:22 lint/style/useNodejsImportProtocol  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  i A Node.js builtin module should be imported with the node: protocol.
  
    1 │ import * as p from "@clack/prompts";
  > 2 │ import { join } from "path";
      │                      ^^^^^^
    3 │ import pc from "picocolors";
    4 │ import { fileExists, writeTemplate } from "../utils/files.js";
  
  i Using the node: protocol is more explicit and signals that the imported module belongs to Node.js.
  
  i Unsafe fix: Add the node: protocol.
  
      1   1 │   import * as p from "@clack/prompts";
      2     │ - import·{·join·}·from·"path";
          2 │ + import·{·join·}·from·"node:path";
      3   3 │   import pc from "picocolors";
      4   4 │   import { fileExists, writeTemplate } from "../utils/files.js";
  

src/steps/copy.ts:94:17 lint/style/useTemplate  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  i Template literals are preferred over string concatenation.
  
    92 │   };
    93 │ 
  > 94 │   p.log.message("\n" + pc.dim("Next steps:"));
       │                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^
    95 │   for (const tool of tools) {
    96 │     p.log.message(`  ${pc.bold(TOOL_LABELS[tool])}: ${steps[tool]}`);
  
  i Unsafe fix: Use a template literal.
  
     92  92 │     };
     93  93 │   
     94     │ - ··p.log.message("\n"·+·pc.dim("Next·steps:"));
         94 │ + ··p.log.message(`\n${pc.dim("Next·steps:")}`);
     95  95 │     for (const tool of tools) {
     96  96 │       p.log.message(`  ${pc.bold(TOOL_LABELS[tool])}: ${steps[tool]}`);
  

src/utils/files.ts:1:59 lint/style/useNodejsImportProtocol  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  i A Node.js builtin module should be imported with the node: protocol.
  
  > 1 │ import { access, constants, mkdir, stat, writeFile } from "fs/promises";
      │                                                           ^^^^^^^^^^^^^
    2 │ import { dirname } from "path";
    3 │ 
  
  i Using the node: protocol is more explicit and signals that the imported module belongs to Node.js.
  
  i Unsafe fix: Add the node: protocol.
  
     1    │ - import·{·access,·constants,·mkdir,·stat,·writeFile·}·from·"fs/promises";
        1 │ + import·{·access,·constants,·mkdir,·stat,·writeFile·}·from·"node:fs/promises";
     2  2 │   import { dirname } from "path";
     3  3 │   
  

src/utils/files.ts:2:25 lint/style/useNodejsImportProtocol  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  i A Node.js builtin module should be imported with the node: protocol.
  
    1 │ import { access, constants, mkdir, stat, writeFile } from "fs/promises";
  > 2 │ import { dirname } from "path";
      │                         ^^^^^^
    3 │ 
    4 │ export async function isDirectory(path: string): Promise<boolean> {
  
  i Using the node: protocol is more explicit and signals that the imported module belongs to Node.js.
  
  i Unsafe fix: Add the node: protocol.
  
     1  1 │   import { access, constants, mkdir, stat, writeFile } from "fs/promises";
     2    │ - import·{·dirname·}·from·"path";
        2 │ + import·{·dirname·}·from·"node:path";
     3  3 │   
     4  4 │   export async function isDirectory(path: string): Promise<boolean> {
  

src/utils/paths.ts:1:25 lint/style/useNodejsImportProtocol  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  i A Node.js builtin module should be imported with the node: protocol.
  
  > 1 │ import { homedir } from "os";
      │                         ^^^^
    2 │ import { resolve } from "path";
    3 │ 
  
  i Using the node: protocol is more explicit and signals that the imported module belongs to Node.js.
  
  i Unsafe fix: Add the node: protocol.
  
     1    │ - import·{·homedir·}·from·"os";
        1 │ + import·{·homedir·}·from·"node:os";
     2  2 │   import { resolve } from "path";
     3  3 │   
  

src/utils/paths.ts:2:25 lint/style/useNodejsImportProtocol  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  i A Node.js builtin module should be imported with the node: protocol.
  
    1 │ import { homedir } from "os";
  > 2 │ import { resolve } from "path";
      │                         ^^^^^^
    3 │ 
    4 │ export function expandTilde(input: string): string {
  
  i Using the node: protocol is more explicit and signals that the imported module belongs to Node.js.
  
  i Unsafe fix: Add the node: protocol.
  
     1  1 │   import { homedir } from "os";
     2    │ - import·{·resolve·}·from·"path";
        2 │ + import·{·resolve·}·from·"node:path";
     3  3 │   
     4  4 │   export function expandTilde(input: string): string {
  

Checked 19 files in 10ms. No fixes applied.
Found 14 infos.
