# Secretlint path-based allowlist.
# Syntax: gitignore-style globs. See https://secretlint.github.io/

# Templates contain placeholder tokens (e.g. "xoxb-xxx") for user substitution,
# not real secrets. Ported from the Phase-3 .gitleaks.toml allowlist.
packages/templates/**

# Build and cache artifacts
node_modules/**
dist/**
coverage/**
.cache/**
.turbo/**
.worktrees/**

# Binary / generated files that cannot contain meaningful secrets
bun.lock
