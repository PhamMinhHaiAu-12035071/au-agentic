node_modules/
packages/cli/dist/
.DS_Store
/examples/
.superpowers/

# Toolchain phase 1
coverage/
lcov.info
*.tsbuildinfo

# Project-scope cache (Phase 4 introduces .cache/; ignore now to avoid accidental commits)
.cache/

# Turbo per-package cache artifacts
.turbo/

# Git worktrees — isolated feature workspaces should never be committed.
# Broad rule subsumes earlier benchmark-specific ignores.
.worktrees/

# Scaffolded by au-agentic — re-install on demand via the CLI, never commit
.cursor/
.claude/
.agents/
