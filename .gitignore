node_modules/
packages/cli/dist/
.DS_Store
/examples/
.superpowers/

# Toolchain phase 1
coverage/
lcov.info
*.tsbuildinfo

# Project-scope cache (Phase 4 introduces .cache/; ignore now to avoid accidental commits)
.cache/

# Upstream clone workspace for sync:upstream-patterns script — never commit.
.tmp/

# Turbo per-package cache artifacts
.turbo/

# Git worktrees — isolated feature workspaces should never be committed.
# Broad rule subsumes earlier benchmark-specific ignores.
.worktrees/

# Scaffolded by au-agentic — re-install on demand via the CLI, never commit
.cursor/
.claude/
.agents/

# Codegen output — regenerated on prebuild
packages/cli/src/generated/

# cursor-agent-bench raw artifacts (JSONL per run)
coverage/cursor-bench/
