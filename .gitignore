node_modules/
packages/cli/dist/
.DS_Store
/examples/
.superpowers/

# Toolchain phase 1
coverage/
lcov.info
*.tsbuildinfo

# Project-scope cache (Phase 4 introduces .cache/; ignore now to avoid accidental commits)
.cache/

# Turbo per-package cache artifacts
.turbo/

# Worktree benchmark scratch (Phase 4 introduces .worktrees/.bench)
.worktrees/.bench/
.worktrees/.cold-*/
